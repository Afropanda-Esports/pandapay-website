# WhatsApp business number (digits only, with country code — no +)
# Vite embeds this value at build time. Use digits only, including the country code.
VITE_WHATSAPP_NUMBER=2347043864368

# Production API base URL (optional — dev uses /api proxy to localhost:3000)
# VITE_API_URL=https://api.pandapay.africa
